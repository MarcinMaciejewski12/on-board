import { LoginButtons } from "./ButtonStyles";

export const LoginButton = (props: any)=> {
    return (
        <LoginButtons whileHover={{scale: 1.1}}>Zaloguj się</LoginButtons>
    )
}

export default LoginButton;